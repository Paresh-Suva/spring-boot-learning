spring.application.name=csrf-demo

# CSRF ENABLED (DEFAULT): POST /users WITHOUT CSRF TOKEN -> 403 FORBIDDEN
# SET TO false TO DISABLE CSRF FOR THIS DEMO ONLY, THEN POST /users SUCCEEDS
app.security.csrf.enabled=false
